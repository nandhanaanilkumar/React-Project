const NetworkTab = () => {
  return (
    <div
      style={{
        background: "#fff",
        padding: "12px 16px",
        borderRadius: "10px",
        border: "1px solid #004182",
        display: "flex",
        gap: "20px",
        marginBottom: "16px",
      }}
    >
      <span style={{ color: "#004182", fontWeight: 600 }}>Grow</span>
      <span style={{ color: "#555" }}>Catch up</span>
    </div>
  );
};

export default NetworkTab;
